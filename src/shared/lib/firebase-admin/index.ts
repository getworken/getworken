/**
 * Firebase Admin SDK public API
 * @module shared/lib/firebase-admin
 * 
 * ⚠️ SERVER-ONLY MODULE
 * This module MUST NOT be imported from client-side code
 */

import 'server-only';

export * from './config';
export { adminAuth, adminDb } from './config';
